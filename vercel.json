{
    "rewrites": [{ "source": "/(.*)", "destination": "/", "builds": [
        {
        "src": "./server.js",
        "use": "@now/node"
        }
        ],
       "routes": [
       {
        "src": "/(.*)",
         "dest": "/"
        }
       ]}]
}